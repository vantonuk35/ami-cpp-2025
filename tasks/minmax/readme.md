# MinMax

Напишите функцию MinMaxElement(begin, end), принимающую на вход итераторы на начало и конец контейнера и возвращающую пару итераторов, указывающих на минимальноый и максимальный элемент из контейнера.
